Notes: 0.1

LocalMod cached
ModDownloadItem data-only
ModDownloadItemInstallInfo data-only

Actions SocialBrowserModule:
Search: QueryParameters -> Iterator of ModDownloadItem
Search: ModId -> ModDownloadItem
Local dynamic info (like is installed, need update etc...): ModDownloadItem -> ModDownloadItemInstallInfo
GetModFromLocal: LocalMod -> ModId? <used to check if it's owned too>
GetDeps: HashMap<ModId> -> HashMap<ModId|ModDownloadItem>
Install: HashMap<ModId> -> void?? (some progress handler) (should inform OnLocalModsChanged)
Uninstall: ModId -> void
??? Publish should get a mod definition and not buildData... should ONLY contain non LocalMod defined stuff
Publish: LocalMod, NameValueCollection extraBuildData, WorkshopItemPublishSettings -> bool


Actions LocalMod handler (ModOrganizer?):
Uninstall: LocalMod -> void (should call the Uninstall of the social browser if it's from there)
OnLocalModsChanged: event (passing the list of slugs of updated mods, or called for each mod)
