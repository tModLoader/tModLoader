--- src/TerrariaNetCore/Terraria/WorldSections.cs
+++ src/Unified/Terraria/WorldSections.cs
@@ -83,6 +_,24 @@
 
 	public bool TileLoaded(int tileX, int tileY) => SectionLoaded(Netplay.GetSectionX(tileX), Netplay.GetSectionY(tileY));
 
+	public bool TilesLoaded(int startX, int startY, int endXInclusive, int endYInclusive)
+	{
+		int sX = Netplay.GetSectionX(startX);
+		int sY = Netplay.GetSectionY(startY);
+		int eX = Netplay.GetSectionX(endXInclusive);
+		int eY = Netplay.GetSectionY(endYInclusive);
+
+		for (int x = sX; x <= eX; x++) {
+			for (int y = sY; y <= eY; y++) {
+				if (!SectionLoaded(x, y)) {
+					return false;
+				}
+			}
+		}
+
+		return true;
+	}
+
 	public bool SectionLoaded(int x, int y)
 	{
 		if (x < 0 || x >= width)
@@ -128,7 +_,10 @@
 
 	public void SetSectionFramed(int x, int y)
 	{
+		/*
 		if (x >= 0 && x < width && y >= 0 && y < height) {
+		*/
+		if (x >= 0 && x < width && y >= 0 && y < height && data[y * width + x][0] && !data[y * width + x][1]) {
 			BitsByte bitsByte = data[y * width + x];
 			if (bitsByte[0] && !bitsByte[1]) {
 				bitsByte[1] = true;
