--- src/TerrariaNetCore/Terraria/UI/ItemTooltip.cs
+++ src/tModLoader/Terraria/UI/ItemTooltip.cs
@@ -29,6 +_,13 @@
+	// Added by TML
+	private ItemTooltip(LocalizedText text)
+	{
+		_text = text;
+	}
+
 	private ItemTooltip(string key)
 	{
 		_text = Language.GetText(key);
 	}
 
+	// TML: Made internal
-	public static ItemTooltip FromLanguageKey(string key) => new ItemTooltip(key);
+	internal static ItemTooltip FromLanguageKey(string key) => new ItemTooltip(key);
@@ -35,3 +_,6 @@
+
+	// Added by TML
+	public static ItemTooltip FromLocalization(LocalizedText text) => new(text);
 
 	public string GetLine(int line)
 	{
