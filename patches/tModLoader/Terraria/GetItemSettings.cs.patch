--- src/TerrariaNetCore/Terraria/GetItemSettings.cs
+++ src/tModLoader/Terraria/GetItemSettings.cs
@@ -1,7 +_,14 @@
 using System;
+using Microsoft.Xna.Framework;
 
 namespace Terraria;
 
+/// <summary>
+/// Contains settings for the popup text shown when using <see cref="Player.GetItem(Terraria.Item, Terraria.GetItemSettings)"/>. The contained static fields can be used to match vanilla behavior for various actions.
+/// <para/> <b>LongText:</b> <inheritdoc cref="PopupText.NewText(PopupTextContext, Item, Vector2, int, bool , bool)" path="/param[@name='longText']"/>
+/// <br/> <b>NoText:</b> If <see langword="true"/>, no <see cref="PopupText"/> will spawn.
+/// <br/> <b>CanGoIntoVoidVault:</b> If <see langword="true"/>, the item can be placed into the Void Vault.
+/// </summary>
 public struct GetItemSettings
 {
 	public static GetItemSettings GiftRecieved = new GetItemSettings(LongText: true);
@@ -21,6 +_,7 @@
 	public readonly bool NoCoinMerge;
 	public readonly Action<Item> StepAfterHandlingSlotNormally;
 
+	/// <inheritdoc cref="GetItemSettings"/>
 	public GetItemSettings(bool LongText = false, bool NoText = false, bool CanGoIntoVoidVault = false, bool NoSound = false, Action<Item> StepAfterHandlingSlotNormally = null, bool NoCoinMerge = false)
 	{
 		this.LongText = LongText;
