--- src/TerrariaNetCore/Terraria/GameContent/UI/NewCraftingUI.cs
+++ src/tModLoader/Terraria/GameContent/UI/NewCraftingUI.cs
@@ -392,7 +_,7 @@
 				HandleCraftSlot(recipeEntry, 42);
 
 			spriteBatch.Draw(Main.Assets.Request<Texture2D>("Images/UI/Craft").Value, vector + new Vector2(47f, 13f), null, Color.White, 0f, Vector2.Zero, 0.8f, SpriteEffects.None, 0f);
-			for (; i < Recipe.maxRequirements && !recipe.requiredItem[i].IsAir; i++) {
+			for (; i < Recipe.maxVanillaRequirements && !recipe.requiredItem[i].IsAir; i++) {
 			}
 
 			vector += new Vector2(72f, 22f);
@@ -642,7 +_,8 @@
 		UILinkPoint rightSide = ((materialCount == 0) ? null : uILinkPage.LinkMap[20001]);
 		UILinkPoint uILinkPoint10 = ((materialCount == 0) ? null : uILinkPage.LinkMap[20001 + materialCount - 1]);
 		_helper.PairLeftRight(uILinkPoint2, rightSide);
+		// TODO: Adjust to specific recipe if more than 15?
-		for (int j = 0; j < Recipe.maxRequirements - 1; j++) {
+		for (int j = 0; j < Recipe.maxVanillaRequirements - 1; j++) {
 			UILinkPoint uILinkPoint11 = uILinkPage.LinkMap[20001 + j];
 			_helper.PairLeftRight(uILinkPoint11, uILinkPage.LinkMap[20001 + j + 1]);
 			uILinkPoint11.Down = uILinkPoint2?.Down ?? (-1);
