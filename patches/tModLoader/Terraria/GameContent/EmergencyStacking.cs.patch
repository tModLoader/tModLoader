--- src/TerrariaNetCore/Terraria/GameContent/EmergencyStacking.cs
+++ src/tModLoader/Terraria/GameContent/EmergencyStacking.cs
@@ -3,6 +_,7 @@
 using System.Linq;
 using Microsoft.Xna.Framework;
 using Terraria.ID;
+using Terraria.ModLoader;
 using Terraria.Testing;
 
 namespace Terraria.GameContent;
@@ -88,7 +_,8 @@
 
 		public int NumToTransfer {
 			get {
-				if (!Item.CanStack(src.inner, dst.inner))
+				// TML: Recheck CanStackInWorld, dst may have been redirected since the transfer was planned.
+				if (!Item.CanStack(src.inner, dst.inner) || !ItemLoader.CanStackInWorld(dst, src))
 					return 0;
 
 				return Math.Min(src.stack, dst.maxStack - dst.stack);
@@ -140,7 +_,7 @@
 				item.StackingPriority = count--;
 			}
 
-			_groupLookup = (from t in Enumerable.Range(0, ItemID.Count)
+			_groupLookup = (from t in Enumerable.Range(0, ItemLoader.ItemCount)
 							select PreservationOrder.First((Group g) => g.Contains(ContentSamples.ItemsByType[t]))).ToArray();
 
 			return _groupLookup;
@@ -156,6 +_,8 @@
 		return PendingTransfers.Any((Transfer t) => t.src == item || t.dst == item);
 	}
 
+	public static void ResetGroupLookup() => _groupLookup = null;
+
 	public static void ClearPendingTransfersInvolving(WorldItem item)
 	{
 		if (PendingTransfers.Count != 0) {
@@ -267,6 +_,9 @@
 				if (stackableItem.type != other.type || stackableItem.item == other.item || stackableItem.IsPreferredDestination(other) || !Item.CanStack(stackableItem.item.inner, other.item.inner))
 					continue;
 
+				if (!ItemLoader.CanStackInWorld(destination: other.item, source: stackableItem.item))
+					continue;
+
 				int num = DistanceBetween(stackableItem.item, other.item);
 				if (num <= MaxTransferDistance) {
 					Group group = GroupLookup[stackableItem.type];
@@ -300,8 +_,11 @@
 
 		int numToTransfer = t.NumToTransfer;
 		if (numToTransfer != 0) {
+			/* TML: Use StackItems so OnStack hook fires.
 			src.stack -= numToTransfer;
 			dst.stack += numToTransfer;
+			*/
+			ItemLoader.StackItems(dst.inner, src.inner, out _, numToTransfer: numToTransfer);
 			if (src.stack <= 0)
 				src.TurnToAir();
 
