--- src/Terraria/Terraria/GameContent/PlayerSleepingHelper.cs
+++ src/tModLoader/Terraria/GameContent/PlayerSleepingHelper.cs
@@ -1,6 +_,7 @@
 using Microsoft.Xna.Framework;
 using System;
 using Terraria.ID;
+using Terraria.ModLoader;
 
 namespace Terraria.GameContent
 {
@@ -175,6 +_,7 @@
 
 			anchorPosition = new Point(num4, num + 1).ToWorldCoordinates(8f, 16f);
 			visualoffset = SetOffsetbyBed(tileSafely.frameY / 36);
+			TileLoader.ModifySleepingOffset(x, y, tileSafely.type, ref visualoffset);
 			return true;
 		}
 
