--- src/TerrariaNetCore/Terraria/GameContent/Achievements/CustomIntCondition.cs
+++ src/tModLoader/Terraria/GameContent/Achievements/CustomIntCondition.cs
@@ -4,6 +_,10 @@
 
 namespace Terraria.GameContent.Achievements;
 
+/// <summary>
+/// A custom condition that tracks an int value. Must be manually adjusted by incrementing <see cref="Value"/> directly. The condition will complete automatically when the supplied max value is reached.
+/// <br/><br/> If multiple custom conditions are used for an achievement, a unique key should be provided.
+/// </summary>
 public class CustomIntCondition : AchievementCondition
 {
 	[JsonProperty("Value")]
@@ -25,7 +_,7 @@
 		}
 	}
 
-	private CustomIntCondition(string name, int maxValue)
+	public CustomIntCondition(string name, int maxValue)
 		: base(name)
 	{
 		_maxValue = maxValue;
