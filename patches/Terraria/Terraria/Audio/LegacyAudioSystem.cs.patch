--- src/decompiled/Terraria/Audio/LegacyAudioSystem.cs
+++ src/Terraria/Terraria/Audio/LegacyAudioSystem.cs
@@ -69,6 +_,7 @@
 			if (!contentSource.HasAsset(assetPath))
 				continue;
 
+#if !SERVER
 			string extension = contentSource.GetExtension(assetPath);
 			try {
 				IAudioTrack audioTrack2 = null;
@@ -94,6 +_,7 @@
 				Main.IssueReporter.AddReport(textToShow);
 				Main.IssueReporterIndicator.AttemptLettingPlayerKnow();
 			}
+#endif
 		}
 
 		return audioTrack;
@@ -115,6 +_,9 @@
 	public void Init(string renderer = "")
 	{
 		Engine = new AudioEngine("Content\\TerrariaMusic.xgs", TimeSpan.FromMilliseconds(250.0), renderer);
+#if FNA
+		Console.WriteLine("SDL Audio Driver: " + SDL3.SDL.SDL_GetCurrentAudioDriver());
+#endif
 		SoundBank = new SoundBank(Engine, "Content\\Sound Bank.xsb");
 		Engine.Update();
 		WaveBank = new WaveBank(Engine, "Content\\Wave Bank.xwb", 0, 512);
